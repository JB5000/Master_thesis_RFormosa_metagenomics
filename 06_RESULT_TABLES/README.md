# Result tables

This directory contains nine scientific result tables with descriptive file
names. `data/` holds the primary tables and `figure_plotted_values/` holds
exact matrices used in final figures. Missing information is not converted to
zero; explicitly absent BLASTn target/date combinations are recorded as zero
only where documented by the export workflow.
