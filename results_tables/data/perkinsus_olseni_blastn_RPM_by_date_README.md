# *Perkinsus olseni* BLASTn-derived RPM by sampling date

This is the current table for the BLASTn version of Figure 3.12.  For each of the 23 sampling dates, the values are the mean and standard deviation across the three equal-depth subsamples.  They were copied from `17_BLAST_VALIDATION/Perkinsus_olseni_BLAST_hits_timeseries.tsv`, which is also the input used by `make_Fig_3.12_perkinsus.py`.

This BLASTn-derived table is the sole Perkinsus time-series table to use with the current Figure 3.12.
